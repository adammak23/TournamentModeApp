<Page x:ConnectionId='1' x:Name="page" x:Class="WindowsApp2.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:WindowsApp2.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:WindowsApp2.ViewModels"
      xmlns:vml="using:WindowsApp2.ViewModels"
      mc:Ignorable="d">

    <Page.DataContext>
        <vm:MainPageViewModel x:ConnectionId='2' x:Name="ViewModel" />
    </Page.DataContext>

    <Page.Resources>
        <vml:MainPageViewModel x:ConnectionId='3' x:Key="ViewModel2" x:Name="ViewModel2" />
    </Page.Resources>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='4' x:Name="AdaptiveVisualStateGroup">
                <VisualState x:ConnectionId='5' x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for narrow view  -->
                        <Setter Target="stateTextBox.Text" Value="Narrow Visual State" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='6' x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for normal view  -->
                        <Setter Target="stateTextBox.Text" Value="Normal Visual State" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='7' x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for wide view  -->
                        <Setter Target="stateTextBox.Text" Value="Wide Visual State" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:PageHeader x:ConnectionId='8' x:Name="pageHeader"
                             RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True"
                             Text="Main Page">

            <!--  secondary commands  -->
            <controls:PageHeader.SecondaryCommands>
                <AppBarButton x:ConnectionId='16'                                         Label="Settings" />
                <AppBarButton x:ConnectionId='17'                                        Label="Privacy" />
                <AppBarButton x:ConnectionId='18'                                      Label="About" />
            </controls:PageHeader.SecondaryCommands>

        </controls:PageHeader>

        <RelativePanel EntranceNavigationTransitionInfo.IsTargetElement="True"
                       RelativePanel.AlignBottomWithPanel="True"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       RelativePanel.Below="pageHeader">

            <TextBlock x:ConnectionId='14' x:Name="stateTextBox"
                       Margin="359,128,-343,-112"
                       Text="Current Visual State" />
            <TextBlock x:ConnectionId='15' x:Name="Error1" Text="{Binding Error, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="189,15,-326,-130" />
        </RelativePanel>
        <Grid x:ConnectionId='9' Margin="10,53,-221,-188" x:Name="grid">
            <TextBox x:ConnectionId='10' MinWidth="150"
                         x:Name="Login"
                         PlaceholderText="Login"
                         MinHeight="30"
                         Text="{Binding Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="10,10,72,0" Height="32" VerticalAlignment="Top">
                <!--Text in TextBox only for for DetailsPage binding-->
                <Interactivity:Interaction.Behaviors>
                    <!-- somehow works too: Text="{x:Bind ViewModel.Login, Mode=TwoWay}" -->
                    <!--  enable submit on enter key  -->
                    <Behaviors:KeyBehavior Key="Enter">
                        <Core:CallMethodAction MethodName="GotoDetailsPage" TargetObject="{Binding}" />
                    </Behaviors:KeyBehavior>
                    <!--  focus on textbox when page loads  -->
                    <Core:EventTriggerBehavior>
                        <Behaviors:FocusAction />
                    </Core:EventTriggerBehavior>
                    <!-- popup for loggining maybe
                    <Core:EventTriggerBehavior EventName="Tapped" SourceObject="{Binding TryLogin}">
                        <Behaviors:MessageDialogAction Title="Show profile" Content="This is an example of a Command-type hamburger button. It does not navigate, only raises the Tapped event for some custom implementation." />
                    </Core:EventTriggerBehavior>-->
                </Interactivity:Interaction.Behaviors>
            </TextBox>
            <PasswordBox x:ConnectionId='11' MinWidth="150"
                     x:Name="Password"
                     PlaceholderText="Password"
                     MinHeight="30"
                     Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10,47,72,0" Height="32" VerticalAlignment="Top">
            </PasswordBox>
            <ProgressBar x:ConnectionId='12' x:Name="progressbar" IsIndeterminate="False" Margin="10,80,72,45" Foreground="#FF008BFF" Background="Transparent" Height="9"/>
            <Button x:ConnectionId='13' x:Name="submitButton2"
                    Command="{Binding TryLogin, Source={StaticResource ViewModel2}}" 
                    CommandParameter="{Binding ElementName=page}"
                    Content="Login" Margin="10,93,0,10"
                    >
                <!--IsEnabled="{Binding ElementName=Login, Path=Text.Length, Mode=OneWay}"-->
                <!--click when logged in Click="{x:Bind ViewModel.GotoDetailsPage}" -->
            </Button>
        </Grid>
    </RelativePanel>
</Page>


